- name: PQC Migration Step 9
  hosts: in_vehicle_devices
  gather_facts: false
  vars:
    migrated_nodes:
    - Server_Backend.K_pr14
  tasks:
  - name: Deploy Post-Quantum Cryptographic Assets & Keys
    ansible.builtin.copy:
      content: '# PQC Migrated Keys and Algorithms

        # Nodes: Server_Backend.K_pr14'
      dest: /etc/pqc/keys_step_9.conf
